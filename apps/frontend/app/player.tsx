import { ScrollView, View } from "react-native";

export default function Player() {
  return (
    <ScrollView
      style={{
        flexDirection: "column",
        flex: 1,
      }}
    ></ScrollView>
  );
}
