import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "column",
    flex: 1,
  },

  safeArea: {
    paddingTop: 60,
  },

  content: {
    gap: 20,
    paddingVertical: 20,
  },
});
